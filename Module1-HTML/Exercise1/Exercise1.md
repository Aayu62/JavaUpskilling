# Exercise 1 Notes

This page uses the HTML5 base structure with `<!DOCTYPE html>`, `lang="en"`, and `meta charset="UTF-8"`.
The semantic sections make the document easier to read in DevTools and easier to maintain later.

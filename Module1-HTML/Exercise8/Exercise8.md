# Exercise 8 Notes

`localStorage` persists after the browser closes, while `sessionStorage` lasts only for the current tab session.
Loading saved preferences back into the form gives returning users a smoother experience.
